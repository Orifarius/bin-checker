services:
  bin-checker:
    build: .
    container_name: bin-checker
    restart: unless-stopped
    ports:
      - "5050:5000"
    environment:
      - APP_PASSWORD=yourpassword
      - HANDY_API_TOKEN=yourapikey
      - FLASK_SECRET_KEY=sessionkey-changetorandomstring
